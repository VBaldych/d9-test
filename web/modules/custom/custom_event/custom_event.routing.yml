# Define a route for our event dispatching example.
custom_event.form:
  path: '/custom-event'
  defaults:
    _form: '\Drupal\custom_event\Form\CustomEventForm'
    _title: 'Custom Submit Event Form'
  requirements:
    _permission: 'access content'
